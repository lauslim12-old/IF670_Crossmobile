import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '32827-NicholasDwiartoW',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
