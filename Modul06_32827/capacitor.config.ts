import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Modul06_32827',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
