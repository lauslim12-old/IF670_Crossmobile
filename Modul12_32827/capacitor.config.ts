import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "umn.ac.id.modul12",
  appName: "if670-module12",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
