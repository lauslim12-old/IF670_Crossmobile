import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ac.id.umn.tigaduadelapanduatujuh.moduldelapan',
  appName: 'Modul08_32827',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
