import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ac.id.umn.crossmobile',
  appName: 'Modul10_32827',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
